# Espelho de pyproject.toml (fonte da verdade). Instale com:
#   pip install -e ".[dev,vision]"      (recomendado)
# ou, se preferir este arquivo:  pip install -r requirements.txt
torch>=2.2
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
statsmodels>=0.14
botorch>=0.10
gpytorch>=1.11
structlog>=24.1
pytest>=7.4
pytest-cov>=4.0
torchvision>=0.17
